{
  "name": "sri-krishna-builders",
  "version": "1.0.0",
  "private": true,
  "description": "SRI KRISHNA BUILDERS offline billing app",
  "scripts": {
    "build:web": "node -e \"console.log('Static web app: no build step required')\"",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "typescript": "^5.8.3"
  }
}
